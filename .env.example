QF_CLIENT_ID=
# QF_CLIENT_SECRET is only needed for confidential clients (server-side exchange).
# Leave empty if Quran Foundation provisioned your client as public.
QF_CLIENT_SECRET=
# "prelive" (default) or "production"
QF_ENV=prelive
